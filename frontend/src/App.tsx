export function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">Autologbook</p>
        <h1>Frontend scaffold is running</h1>
        <p className="lead">
          The frontend container now has a valid React and TypeScript
          project and can start with Vite.
        </p>
      </section>
    </main>
  );
}
